بسم الله الرحمن الرحیم

# مستند طراحی تجربه کاربری (UX Design Document)

## ۱. هویت و ساختار کلی (Architecture)

- **نام پروژه:** سامانه پیدا (مدیریت گمشدگان مناسبتی)
    
- **زبان طراحی:** مینیمال، مبتنی بر فرم‌های خرد شده (Duolingo Style) با هدف کاهش استرس و بار شناختی.
    
- **پلتفرم:** اپلیکیشن موبایل (react native).
    

---

## ۲. جریان ورود و احراز هویت (Onboarding & Auth)

### الف) گالری آشنایی (Onboarding Slides)

- **تعداد:** ۳ تا ۵ اسلاید بصری.
    
- **محتوا:** معرفی کوتاه «ثبت سریع»، «تطبیق هوشمند» و «ارتباط مستقیم با خادمان».
    
- **تعامل:** دکمه «شروع» در اسلاید آخر که به فرم ورود هدایت می‌شود.
    

### ب) فرم ورود (Login)

- **فیلدها:** انتخاب کشور (Flag + Code) + فیلد شماره موبایل.
    
- **ارسال کد:** استفاده از پیام‌رسان‌های **ایتا** و **واتساپ** برای ارسال OTP (کد یک‌بار مصرف).
    
- **منطق خطا:** امکان ۳ بار تلاش برای ورود کد؛ در صورت شکست یا اتمام زمان، بازگشت به مرحله وارد کردن شماره موبایل.
    

---

## ۳. صفحه اصلی و داشبورد عملیاتی

- **دکمه طلایی (CTA):** یک دکمه بزرگ در پایین صفحه برای «ثبت گمشده / پیدا شده».
    
- **آمار زنده:** نمایش تعداد وصال‌های موفق و افراد پیدا شده (ایجاد حس امید و اعتماد).

---

## ۳.۱. نوار پایین (Bottom Navigation Bar)

برای دسترسی آسان به بخش‌های اصلی اپلیکیشن، یک نوار پایین ثابت با آیتم‌های زیر طراحی می‌شود:

| **آیکن** | **عنوان**    | **دسترسی**             | **توضیحات**                       |
| -------- | ------------ | ---------------------- | --------------------------------- |
| **🏠**   | خانه         | همه                    | صفحه اصلی و داشبورد عملیاتی       |
| **🏕️**  | موکب من      | همه                    | ثبت موکب یا مشاهده وضعیت موکب و آمار     |
| **📋**   | گزارش‌ها | همه                    | جستجو، فیلتر و لیست گزارش‌ها (دسترسی بر اساس نقش) |

- **منطق نمایش:** آیکن‌های غیرقابل دسترسی به صورت خاکستری و غیرفعال نمایش داده می‌شوند.
- **نشانگر فعال:** آیکن صفحه فعلی با رنگ تم اپلیکیشن مشخص می‌شود.

---

## ۳.۲. صفحه موکب من (My Mawkab)

این صفحه برای همه کاربران قابل دسترسی است و محتوای آن بر اساس وضعیت ثبت موکب نمایش داده می‌شود:

### حالت اول: کاربر هنوز موکب ثبت نکرده است

- **CTA اصلی:** دکمه بزرگ «ثبت موکب» با تم جذاب
- **محتوای کمکی:** توضیحات کوتاه درباره مزایای ثبت موکب (دسترسی به جستجو، مدیریت بهتر, etc.)
- **مراحل ثبت:** هدایت کاربر به فرم چند مرحله‌ای ثبت موکب

---

## ۳.۳. فرم ثبت موکب (Mawkab Registration Form)

فرم ثبت موکب به صورت **۲ مرحله‌ای** با **Progress Bar** در بالا طراحی می‌شود:

### مرحله ۱: اطلاعات پایه
- **نام موکب:** فیلد متنی برای نام رسمی موکب
- **نام موکب‌دار:** نام و نام خانوادگی شخص مسئول
- **شماره موبایل:** پیش‌فرض شماره موبایل کاربر (قابل تغییر)

### مرحله ۲: موقعیت مکانی
- **انتخاب موقعیت:** نمایش نقشه با پین روی موقعیت فعلی GPS
- **قابلیت جابجایی:** کاربر می‌تواند پین را به موقعیت دقیق موکب منتقل کند
- **آدرس متنی:** شماره عمود، مشخصات شاخص دیگر (مثلاً عمود ۸۰)

**نکته:** تمام اطلاعات هر دو مرحله در یک درخواست به بکند ارسال می‌شود.

### پس از ثبت:
- **پیام تایید:** «موکب شما با موفقیت ثبت شد و در انتظار تایید است»
- **هدایت:** بازگشت به صفحه «موکب من» با نمایش وضعیت «در انتظار تایید»

### حالت دوم: کاربر موکب ثبت کرده است

- **وضعیت موکب:** نمایش واضح وضعیت فعلی:
  - **🟡 در انتظار تایید:** باکس زرد با توضیحات فرآیند بررسی
  - **🟢 تایید شده:** نوار سبز با دسترسی کامل به امکانات
- **جزئیات موکب:** نمایش اطلاعات ثبت شده (نام، آدرس، تماس)
- **آمار موکب:** نمایش تعداد گزارش‌های ثبت شده و تعداد گزارش‌های حل‌شده
- **دکمه‌های مدیریت:** ویرایش اطلاعات، مشاهده گزارش‌های مرتبط
    

---

## ۴. تجربه ثبت گزارش (Registration Journey)

استفاده از فرم‌های **۴ مرحله‌ای** با **Progress Bar** در بالا:

### مرحله ۱ از ۴: اطلاعات پایه
- **انتخاب نوع:** گمشده (تم قرمز ملایم) / پیدا شده (تم سبز)
- **نام و نام خانوادگی:** (اختیاری)
- **جنسیت:** پسر / دختر (آیکونیک)
- **سن:** فیلد عددی

### مرحله ۲ از ۴: تصاویر
- **آپلود عکس:** ۰ تا ۵ عکس
- **انتخاب:** گرفتن عکس با دوربین یا انتخاب از گالری
- **فرایند:** هر عکس جداگانه آپلود و تایید می‌شود (presigned URL به S3)
- **امکان حذف:** کاربر می‌تواند عکس‌های انتخاب‌شده را حذف کند

### مرحله ۳ از ۴: موقعیت مکانی
- **نقشه:** نمایش نقشه با پین خودکار روی موقعیت GPS فعلی (قابل جابجایی)
- **جزئیات موقعیت:** فیلد متنی برای آدرس نوشتاری (مثلاً عمود ۸۰)

### مرحله ۴ از ۴: اطلاعات تماس و توضیحات
- **شماره تماس:** پیش‌فرض شماره کاربر (قابل تغییر)
- **توضیحات مشخص‌کننده:** فیلد متنی آزاد برای موارد خاص (رنگ لباس، تکیه‌کلام، لهجه و...)

### پس از ثبت:
- تمام اطلاعات ۴ مرحله در **یک درخواست** به بکند ارسال می‌شود
- در پاسخ: شناسه‌های مدیا به URL قابل نمایش تبدیل می‌شوند
- **لیست کاندیداهای مچ:** فهرستی از گزارش‌های سمت مخالف که احتمال تطبیق دارند (می‌تواند خالی باشد)
- **کد پیگیری:** نمایش کد پیگیری و ارسال پیام اطلاع‌رسانی via واتساپ/ایتا
    

---

## ۴.۱. صفحه گزارش‌ها (Reports Page)

این صفحه ادغام جستجو و گزارش‌های من است و برای همه کاربران قابل دسترسی است.

### ساختار صفحه:
- **نوار بالایی:** فیلد جستجو با دکمه ذره‌بین + دکمه فیلتر (آیکون غربال/دنده)
- **لیست گزارش‌ها:** کارت‌های گزارش با اسکرول بی‌نهایت
- **دکمه شناور (FAB):** برای ثبت گزارش جدید

### فیلترها (در Bottom Sheet):
- **جنسیت:** همه / پسر / دختر (پیش‌فرض: همه)
- **نوع:** همه / گمشده / پیداشده (پیش‌فرض: همه)
- **وضعیت:** فعال / حل‌شده / معلق (پیش‌فرض فرانت: فعال، پیش‌فرض بکند: همه)
- **مرتب‌سازی:** جدیدترین / نزدیک‌ترین (پیش‌فرض: جدیدترین)
- **محدوده گزارش‌ها:** (فقط برای موکب‌دار تایید شده قابل تغییر)
  - موکب‌دار تایید شده: می‌تواند همه گزارش‌ها را ببیند
  - سایر کاربران: فقط گزارش‌های خودشان را می‌بینند

### نمایش کارت گزارش:
- برچسب نوع: گمشده / پیداشده
- برچسب وضعیت: فعال / حل‌شده / معلق
- برچسب جنسیت: پسر / دختر
- نام فرد
- سن
- تاریخ ثبت گزارش
- کلیک روی کارت: رفتن به صفحه جزئیات گزارش

---

## ۵. جزئیات گزارش و مرکز تطبیق (Report Details & Matches)

این بخش شامل دو لایه صفحه است:
    

### لایه اول: جزئیات گزارش و لیست تطبیق‌ها

- نمایش اطلاعات کامل فرد ثبت شده (شامل لیست عکس‌ها).
    
- **بخش تطبیق‌های پیشنهادی:** لیستی از موارد «سمت مخالف» که سیستم مچ کرده است.
  - برای هر مچ: زمان ایجاد گزارش، وضعیت (فعال/غیرفعال)، نام، جنسیت، سن، عکس، موقعیت
    
- **دکمه «پیدا شد»:** برای بستن پرونده در صورتی که فرد خارج از اپلیکیشن پیدا شده باشد.
    

### لایه دوم: صفحه جزئیات تطبیق (Match Detail)

- **دسترسی via Deep Link:** این صفحه می‌تواند از طریق لینک عمیق مستقیماً باز شود (وقتی مچینگ رخ می‌دهد)
    
- **مقایسه دوطرفه:** نمایش عکس و مشخصات گزارش کاربر در کنار گزارش طرف مقابل.
    
- **دکمه‌های اقدام:**
    
    - **دکمه تماس:** باز کردن مستقیم شماره‌گیر گوشی.
        
    - **دکمه «این مورد من نیست»:** رد تطبیق و بازگرداندن گزارش به حالت فعال.
        

---

## ۶. منطق ماشین وضعیت (State Machine Logic)

### وضعیت گزارش:
- **فعال (Active):** گزارش در جستجو و مچینگ شرکت می‌کند
- **حل‌شده (Resolved):** فرد پیدا شده و گزارش بسته شده است
- **معلق (Suspended):** ادمین گزارش را به دلیل گذشت زمان معلق کرده است (در مچینگ شرکت نمی‌کند ولی در جستجو قابل مشاهده است)

### وضعیت مچ:
- **پیش‌فرض (Pending):** مچ ایجاد شده و منتظر اقدام است
- **تایید شده (Confirmed):** فعلا نداریم
- **رد شده (Rejected):** طرف گم شده مچ را رد کرده است

### انتقال وضعیت‌ها:

1. **گزارش فعال:** با ثبت گزارش جدید، سیستم با گزارش‌های فعال سمت مخالف مقایسه می‌کند و مچ ایجاد می‌کند.
    
2. **مچ پیش‌فرض ➔ رد شده:** طرف گم شده مچ را رد کرده است
    
3. **گزارش فعال ➔ حل‌شده:** با تایید نهایی وصال یا بستن دستی توسط ثبت‌کننده.
    
4. **گزارش فعال ➔ معلق:** ادمین گزارش را به دلیل گذشت زمان معلق می‌کند.
    

---

## ۷. سیستم اطلاع‌رسانی و فنی

- **Deep Links:** ارسال پیام به ایتا/واتساپ حاوی لینکی که مستقیماً صفحه «جزئیات تطبیق» را باز می‌کند.
    
- **بومی‌سازی:** نمایش تقویم شمسی برای کاربران فارسی‌زبان و میلادی برای سایرین.
    
- **نقشه:** استفاده از نقشه‌های سبک و کم‌حجم برای لود سریع در اینترنت ضعیف.
    

---

### نکته نهایی برای پیاده‌سازی:

این مستند بر اساس اصل **«سرعت در بحران»** تنظیم شده است. تمامی دکمه‌های تماسی و ناوبری باید با ابعاد استاندارد (حداقل ۴۸ پیکسل) طراحی شوند تا در حرکت و لرزش دست به راحتی قابل لمس باشند.
